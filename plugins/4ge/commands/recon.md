---
description: "[REDIRECT] Use /recall instead (memory search, repo map, context budget, deep search)"
argument-hint: ""
paths: ["**"]
---

The /recon command has been absorbed into /recall. Use `/recall <query>` for dev-memory search,
`/recall --map` for the repo index, `/recall --budget` for the context budget, and `/recall --deep`
for expanded search + entity graph.
