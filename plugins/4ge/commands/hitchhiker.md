---
description: "[REDIRECT] Use /recall instead. Knowledge search moved to the Guided Knowledge hub."
argument-hint: ""
paths: ["**"]
---

The /hitchhiker helper is retired. Use `/recall <query>` for memory search and `/recall` for the
Knowledge hub menu. The old seed/update pipeline is gone -- use memory_store directly only when
explicitly asked to persist verified facts.
