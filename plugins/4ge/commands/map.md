---
description: "[REDIRECT] Use /recall --map instead"
argument-hint: ""
paths: ["**"]
---

The /map command has been absorbed into /recall. Use `/recall --map` for full repo index, `/recall --map --category <name>` for filtered view.
