---
description: "[REDIRECT] Use /ship instead"
argument-hint: ""
paths: ["**"]
---

The /commit command has been absorbed into /ship. Use `/ship <message>` for the full verify-commit-push pipeline, or `/ship --no-push <message>` to commit without pushing.
