---
description: "Redirects to /outhouse. Use /outhouse for repository maintenance."
argument-hint: "[flags]"
paths: ["plugins/4ge/**"]
---

`/maintain` has been renamed to `/outhouse`.

Invoke the outhouse command with all arguments: run `/outhouse $ARGUMENTS`.
